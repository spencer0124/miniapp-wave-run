{
  "name": "wave-run-miniapp",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "pnpm --filter @skkuverse/wave-run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "preview": "pnpm --filter @skkuverse/wave-run preview"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  }
}
